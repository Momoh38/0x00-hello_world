#include "main.h"
#include <stdio.h>

/**
 *They will contain the use of the file.
 */


void print_name(char *name, void (*f)(char *));
{
	void print(char *name)
		{putchar("%s/n", name};
	return (print);
}
